# オプション学習トラック — Lean による仕様の形式化

**位置づけ**: 必須成果物ではない**任意の学習トラック**。狙いは2つ——(1) 形式手法（定理証明）を学ぶ、(2) 仕様の曖昧さ・抜けを厳密に炙り出す。AI駆動開発として、人間がオプトインし、AI に Lean の形式化を指揮する形で進める。

到達レベルは **(b) 純粋モデル化**（`human-guide/implementation-principles.md` の A/C/D、および技術選定議論の続き）。実 IO・並行の検証(c) や実装抽出には踏み込まない。BBS の要件（有限状態 CRUD ＋明確なルール）は、この (b) にちょうど収まる。

---

## 1. 何をモデル化するか

- **状態**: `Db`（users / threads / comments。comment は `is_deleted` を持つ）。
- **操作**: 登録・ログイン判定・スレッド作成/削除・コメント作成/削除・表示名変更・検索・ソート・ページネーションを、`StateM Db (Except Error α)` の純粋関数として表す。
- **述語**: パスワード強度・表示名長などのバリデーションを decidable な命題として表す。

`StateM Db` は純粋（状態渡しの糖衣）なので、`do` 記法で書いても中身を**証明できる**。モデルは `#eval` で**実行**もでき、実装のテストオラクルに使える。

## 2. 何を証明するか（BBS の代表的な定理）

各 issue / シナリオに対応づく。

| # | 定理（不変条件・性質） | 対応 |
|---|---|---|
| T1 | スレッド/コメント削除は作成者のみ（他者では状態不変 or error） | 06/08、認可 |
| T2 | コメントが1件でもある（削除済み含む）スレッドは、どんな操作列でも削除されない（到達不能） | 06、帰納的不変条件 |
| T3 | コメント削除後：元本文は検索対象外・表示はプレースホルダ・コメント数には1件残る | 08/11/12 |
| T4 | 表示名変更後、その利用者の全投稿の作成者表示が変更後になる（FK参照なら自明／非正規化なら伝播を証明） | 04/05 |
| T5 | PW強度・表示名長の判定が仕様述語と一致（文字数は Unicode スカラ） | 01/04 |
| T6 | 各ソートキーの比較が全順序（反射・反対称・推移・全）を成す。タイブレークを明示 | 12 |
| T7 | 1ページ10件で、全ページの連結が全件を重複なく被覆し順序を保つ | ページネーション |
| T8 | 登録は既存IDとの重複を拒否し、Db 内のユーザ ID は常に一意（不変条件） | 01 |

## 3. 何を証明"しない"か（境界）

- **並行・DB分離レベル・トランザクション競合**（複数ユーザの同時操作）: モデルは逐次・アトミックを仮定。
- **HTTP・セッションCookie・CSRF・argon2 実暗号**: モデルでは抽象化（例: ハッシュは抽象的な単射関数として扱う）。
- **実装（Rust/TS/…）がモデルを refine すること**: 証明しない → **モデルベーステスト＋レビュー**で担保する残余ギャップ。
- これらを本気で検証するなら (c)＝Dijkstra/Hoare モナド・分離論理（Iris）・F\* だが、BBS には過剰。

## 4. 既存方針との接続

- **A/C/D**: 形式化の対象＝**Calculation の核**（バリデーション・認可述語・ソート比較・ページング計算）＋操作の純粋モデル。実 Action（DB/HTTP/セッション）は境界の外＝"imperative shell"。線引きが一致する。
- **shift-left の極北**: エラーを実行時でなく「証明時」に潰す。`tech-selection-rationale.md` のテーゼの延長。
- **decisions と直結**: 形式化は未規定点を必ず明示させる（例: 一覧のコメント数に削除済みを含めるか／論理削除は最終更新日時を更新するか／ソートのタイブレーク）。決めた内容はそのまま `dev-docs/decisions/` に起票する。
- **モデルベーステスト**: Lean モデルを実装のオラクルにして、同じ入力で実装出力と突き合わせる。

## 5. 進め方（任意トラック）

形式化の2つの見返り（曖昧さ発見／不変条件の証明・オラクル）は別のタイミングでピークするので、**2つのタッチポイントに分ける**。最後にまとめてやらない。

- **タイミング（2タッチポイント）**:
  - **① フェーズ0直後 — 仕様レベルの"軽い"形式化（最大の効果）**: 状態 `Db`・操作・バリデーション述語を書き下し、未規定点（コメント数に削除済みを含むか／論理削除は最終更新日時を動かすか／ソートのタイブレーク等）を露出させる。形式化はスタック非依存なのでフェーズ1を待たない。露出した論点は「修正/上流改訂プロンプト」で `requirements-analysis.md` に反映＋ `dev-docs/decisions/` に起票する。**ここでは証明を完遂しない**（フェーズ0を停滞させない。目的は抜け出し）。
  - **② 実装フェーズ — 機能ごとの深い証明＋オラクル（検証の本番）**: 機能を実装する前に、対応する不変条件（T1〜T8）を証明/精緻化する＝通常の Red より一段強い "Red"。実行可能なモデルは実装のテストオラクルにする。機能単位に区切り、全部を一度にやらない。
  - **原則**: あるルールに依存する機能を実装する前に、そのルールを形式化する（Red-Green-Refactor と噛み合う）。フェーズ2/3（基盤・評価ハーネス整備）では形式化の内容と無関係なのでやらない。
- **置き場所**: 新プロジェクトに **トップレベル `formal/`**（`lake` で独自ビルド）。※これは *AI が書く成果物* なので `human-guide/`（人間専用）とは別扱いで、AI が読み書きしてよい。`docs/`（仕様）・`dev-docs/`（AI作業文書）とも分ける。
- **スターターの指示（例）**:
  - ①（フェーズ0直後・軽く）:
    > `formal/` に Lean 4 プロジェクトを作り、BBS の状態 `Db` と操作を `StateM Db (Except Error _)` でモデル化してください。証明はまだ完遂せず、型・操作・バリデーション述語を書く過程で決めざるを得ない未規定点を洗い出し、`dev-docs/decisions/` に起票してください。
  - ②（実装フェーズ・機能ごと）:
    > いま実装する機能 &lt;NN&gt; に対応する不変条件（例: T2「コメントのある（削除済み含む）スレッドは削除されない」）を `formal/` で証明し、モデルを実装のテストオラクルとして使ってください。

## 6. さらに学ぶ

- Lean 4 公式ドキュメント、"Theorem Proving in Lean 4"、"Functional Programming in Lean"、mathlib。
- Lean のプロパティテスト（Plausible 等）でモデルをオラクル化。
- 別味の比較: **TLA+ / Alloy**（軽い反例探索・仕様の無矛盾確認に強い）、**Dafny / F\***（実装抽出・効果検証に強い）。「仕様の抜けを安く見つける」だけなら Alloy/TLA+ の方が ROI が高いことも。

---

*このトラックは任意。やらなくても本体の開発・評価は成立する。学習価値（形式手法・仕様の厳密化）を取りたいときに追加する。*
